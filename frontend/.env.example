# Base URL of the backend API.
# Leave EMPTY for local development — the Vite dev server proxies /api to localhost:8000.
#
# For production (set this in Vercel → Settings → Environment Variables):
# VITE_API_BASE_URL=https://haemia-backend.onrender.com
VITE_API_BASE_URL=
